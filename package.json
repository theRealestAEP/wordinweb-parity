{
  "name": "wordinweb-parity",
  "private": true,
  "version": "0.1.0",
  "description": "Demo, browser tests, and visual-parity corpus for WordInWeb",
  "license": "PolyForm-Noncommercial-1.0.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w demo",
    "test": "npm run test:e2e",
    "dev": "npm run dev -w demo",
    "test:e2e": "playwright test",
    "parity": "node scripts/parity-compare.mjs",
    "report:snapshot": "node scripts/snapshot-report.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
